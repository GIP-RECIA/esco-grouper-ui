package org.esco.grouperui.web.controllers.groupProperties;

/**
 * Enumeration for typa data in summary (Added, deleted).
 * 
 * @author dMoulron
 */
public enum TypeDataSmmary {
    /** added data. */
    ADDED,
    /** deleted data. */
    DELETED
}
