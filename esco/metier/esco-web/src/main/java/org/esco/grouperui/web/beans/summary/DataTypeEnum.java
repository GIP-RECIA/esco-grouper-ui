package org.esco.grouperui.web.beans.summary;

/**
 * Enum to difine type of data in column.
 * 
 * @author Diogene
 */
public enum DataTypeEnum {
    /** type of label. */
    LABEL,
    /** type of checkbox. */
    CHECKBOX,
    /** type of input. */
    INPUT,
    /** type of select. */
    SELECT
}
